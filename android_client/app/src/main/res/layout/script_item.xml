<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:background="@color/ItemColor"
    android:padding="6dp">

    <ImageView
        android:id="@+id/ScriptIcon"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <LinearLayout
        android:layout_width="290dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="6dp">

        <TextView
            android:id="@+id/script_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/ItemColor"
            android:textSize="@dimen/Main_title_fontsize" />

        <TextView
            android:id="@+id/script_description"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/ItemColor"
            android:textSize="@dimen/Main_description_fontsize" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/btnRemove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/round_button"
        android:contentDescription="Round Button"
        android:onClick="RemoveScript"
        app:srcCompat="@drawable/ic_delete_button" />

</LinearLayout>